const Title = ({ title }) => {
  return (
    <h2 className='text-center text-uppercase mt-4 mb-4 h1' style={{borderBottom: '2px gray solid', borderTop: '3px gray solid'}} >
        {title}
    </h2>
  )
}
export default Title